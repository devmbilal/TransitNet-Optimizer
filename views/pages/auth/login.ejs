<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon" />
    <title>TransitNet Optimizer</title>
    <!-- Import Poppins font from Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <!-- Updated Font Awesome to version 6.5.2 -->
    <script
      src="https://kit.fontawesome.com/c4f8aea8e6.js"
      crossorigin="anonymous"
    ></script>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      crossorigin="anonymous"
    />
    <style>
      body {
        font-family: "Poppins", sans-serif;
        margin: 0;
        background: linear-gradient(135deg, #ff6b6b 0%, #4ecdc4 25%, #45b7d1 50%, #96ceb4 75%, #ffeaa7 100%);
        background-size: 400% 400%;
        animation: gradientShift 15s ease infinite;
        color: #fff;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        padding: 20px 0;
      }
      
      @keyframes gradientShift {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
      }
      
      @keyframes float {
        0%, 100% { transform: translateY(0) rotate(0deg); }
        50% { transform: translateY(-20px) rotate(180deg); }
      }

      .login-container {
        position: relative;
        overflow: hidden;
        width: 450px;
        max-width: 100%;
        border-radius: 8px;
        box-sizing: border-box;
        animation: fadeIn 1s ease-in-out;
        background-color: #fff;
        color: #000;
        padding: 40px;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
        text-align: center;
      }

      .welcome-text {
        color: #f5c518;
        font-size: 14px;
        font-weight: 600; /* Slightly bolder */
        margin-bottom: 20px;
        text-transform: uppercase; /* Ensure uppercase text */
        letter-spacing: 1px; /* Add slight spacing for better match */
      }

      .smiley {
        font-size: 40px;
        margin-bottom: 20px;
      }

      .login-container h2 {
        font-size: 28px;
        font-weight: 700; /* Bold for heading */
        color: #1a3c5e;
        margin-bottom: 10px;
      }

      .login-container p {
        font-size: 14px;
        font-weight: 400; /* Regular weight for subtext */
        color: #666;
        margin-bottom: 30px;
      }

      .form-group {
        position: relative;
        margin-bottom: 20px;
        text-align: left;
      }

      .form-group label {
        display: block;
        margin-bottom: 8px;
        font-size: 14px;
        font-weight: 500; /* Medium weight for labels */
        color: #1a3c5e;
      }
      .form-group input {
        width: 100%;
        padding: 12px 40px 12px 40px;
        border: 1px solid #000; /* Default border color set to black */
        border-radius: 20px;
        box-sizing: border-box;
        font-size: 14px;
        font-family: "Poppins", sans-serif;
      }

      /* Change border color to blue on focus */
      .form-group input:focus {
        border-color: #007bff; /* Blue border on focus */
        outline: none; /* Remove default browser outline */
      }

      .form-group .icon {
        position: absolute;
        left: 10px;
        top: 65%;
        transform: translateY(-50%);
        color: #666;
        cursor: pointer;
        font-size: 16px;
        /* Removed margin properties as they conflict with position: absolute */
      }

      input[type="submit"] {
        background: linear-gradient(135deg, #3b82f6, #1d4ed8);
        color: #ffffff;
        border: none;
        padding: 14px;
        border-radius: 8px;
        cursor: pointer;
        font-size: 16px;
        font-weight: 600;
        transition: all 0.3s ease;
        width: 100%;
        font-family: "Poppins", sans-serif;
        box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
      }

      input[type="submit"]:hover {
        background: linear-gradient(135deg, #1d4ed8, #1e40af);
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(59, 130, 246, 0.4);
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }

      .error {
        color: red;
        text-align: center;
        margin-bottom: 15px;
      }

      /* Style the placeholder text to match the image */
      input::placeholder {
        color: #999;
        font-style: italic;
      }
      .icon {
        margin-top: 4px;
        margin: 3px;
      }
      
      /* Responsive design for mobile */
      @media (max-width: 768px) {
        .login-container {
          flex-direction: column !important;
        }
        
        .login-container > div {
          flex: none !important;
          min-height: 200px;
        }
        
        .login-container > div:first-child {
          padding: 30px 20px !important;
        }
        
        .login-container > div:last-child {
          padding: 30px 20px !important;
        }
      }
    </style>
  </head>

  <body>
    <div class="login-container" style="display:flex; gap:0; max-width:980px; width:100%; padding:0; border-radius:14px; overflow:hidden; box-shadow:0 20px 40px rgba(0,0,0,0.25)">
      <!-- Brand panel -->
      <div style="flex:1; background: linear-gradient(135deg, rgba(255,107,107,0.95) 0%, rgba(78,205,196,0.90) 30%, rgba(69,183,209,0.85) 70%, rgba(150,206,180,0.90) 100%), radial-gradient(circle at 30% 20%, rgba(255,255,255,0.1) 0%, transparent 50%), radial-gradient(circle at 70% 80%, rgba(255,255,255,0.08) 0%, transparent 50%); color:#fff; display:flex; flex-direction:column; justify-content:center; align-items:center; padding:40px; position:relative; overflow:hidden;">
        <div style="font-size:46px; font-weight:700; letter-spacing:0.5px; display:flex; align-items:center; gap:12px;">
          <i class="fas fa-route"></i>
          TransitNet
        </div>
        <div style="opacity:0.9; margin-top:10px; text-align:center; max-width:360px; line-height:1.5;">
          Smarter public transport planning. Optimize routes, visualize mobility, and plan services with confidence.
        </div>
        <!-- Floating particles -->
        <div style="position:absolute; width:6px; height:6px; background:rgba(255,255,255,0.3); border-radius:50%; top:20%; left:15%; animation:float 6s ease-in-out infinite;"></div>
        <div style="position:absolute; width:4px; height:4px; background:rgba(255,255,255,0.4); border-radius:50%; top:70%; left:80%; animation:float 8s ease-in-out infinite reverse;"></div>
        <div style="position:absolute; width:8px; height:8px; background:rgba(255,255,255,0.2); border-radius:50%; top:40%; right:20%; animation:float 7s ease-in-out infinite;"></div>
      </div>
      <!-- Form panel -->
      <div style="flex:1; background:#fff; padding:42px 40px; display:flex; flex-direction:column; justify-content:center;">
        <div class="welcome-text" style="color:#0d6efd">Welcome back</div>
        <h2 style="margin:0 0 6px 0">Sign in to your account</h2>
        <p style="margin:0 0 20px 0; color:#6c757d">Access your workspace to continue planning</p>
        <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger py-2" role="alert"><%= error %></div>
        <% } %>
        <form class="login-form" method="post" action="/login">
          <div class="form-group">
            <label for="email">Email Address</label>
            <input
              type="email"
              id="email"
              name="email"
              required
              autocomplete="off"
              placeholder="e.g. name@organization.gov"
            />
            <i class="fas fa-envelope icon"></i>
          </div>
          <div class="form-group">
            <label for="password">Password</label>
            <input
              type="password"
              id="password"
              name="password"
              required
              autocomplete="off"
              placeholder="Enter your password"
            />
            <i class="fas fa-lock icon" id="togglePassword"></i>
          </div>
          <div class="form-group" style="display:flex; justify-content:space-between; align-items:center; margin-top:10px;">
            <a href="/signup" style="font-size:13px; color:#0d6efd; text-decoration:none;">Create account</a>
            <input type="submit" id="submit" value="Sign In" />
          </div>
        </form>
      </div>
    </div>

    <!-- Institutional Logos Footer -->
    <div style="margin-top: 30px; display: flex; align-items: center; justify-content: center; gap: 40px; opacity: 0.9;">
      <div style="display: flex; flex-direction: column; align-items: center; gap: 8px;">
        <img src="/logos/cs-logo.png" alt="CS Department" style="height: 50px; background: white; padding: 8px; border-radius: 8px;">
        <span style="font-size: 10px; color: #000000; text-align: center; font-weight: 500;">Dept. of Computer Science<br>QAU Islamabad</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center; gap: 8px;">
        <img src="/logos/cda-logo.png" alt="CDA" style="height: 50px; background: white; padding: 8px; border-radius: 8px;">
        <span style="font-size: 10px; color: #000000; text-align: center; font-weight: 500;">Capital Development<br>Authority</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center; gap: 8px;">
        <img src="/logos/RoadSafetyLogo.png" alt="Road Safety" style="height: 50px; background: white; padding: 8px; border-radius: 8px;">
        <span style="font-size: 10px; color: #000000; text-align: center; font-weight: 500;">Road Safety Project<br>GCF-744</span>
      </div>
    </div>

    <script>
      document.getElementById("togglePassword").addEventListener("click", function () {
        const passwordField = document.getElementById("password");
        if (passwordField.type === "password") {
          passwordField.type = "text";
          this.classList.remove("fa-lock");
          this.classList.add("fa-lock-open");
        } else {
          passwordField.type = "password";
          this.classList.remove("fa-lock-open");
          this.classList.add("fa-lock");
        }
      });
    </script>
  </body>
</html>
