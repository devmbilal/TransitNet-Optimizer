<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon" />
    <title>TransitNet Optimizer</title>
    <!-- Import Poppins font from Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <!-- Use Font Awesome CDN instead of kit -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
      integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      crossorigin="anonymous"
    />
    <style>
      body {
        font-family: "Poppins", sans-serif;
        margin: 0;
        overflow: hidden;
        background: linear-gradient(135deg, #ff6b6b 0%, #4ecdc4 25%, #45b7d1 50%, #96ceb4 75%, #ffeaa7 100%);
        background-size: 400% 400%;
        animation: gradientShift 15s ease infinite;
        color: #fff;
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
      }
      
      @keyframes gradientShift {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
      }
      
      @keyframes float {
        0%, 100% { transform: translateY(0) rotate(0deg); }
        50% { transform: translateY(-20px) rotate(180deg); }
      }

      .form-container {
        position: relative;
        overflow: hidden;
        width: 450px;
        max-width: 100%;
        border-radius: 8px;
        box-sizing: border-box;
        animation: fadeIn 1s ease-in-out;
        background-color: #fff;
        color: #000;
        padding: 30px;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
        text-align: center;
      }

      .welcome-text {
        color: #f5c518;
        font-size: 14px;
        font-weight: 600;
        margin-bottom: 10px;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .smiley {
        font-size: 40px;
        margin-bottom: 10px;
      }

      .form-container h2 {
        font-size: 28px;
        font-weight: 700;
        color: #1a3c5e;
        margin-bottom: 5px;
      }

      .form-container p {
        font-size: 14px;
        font-weight: 400;
        color: #666;
        margin-bottom: 15px;
      }

      .form-group {
        position: relative;
        margin-bottom: 10px;
        text-align: left;
      }

      .form-group-row {
        display: flex;
        gap: 15px;
        margin-bottom: 10px;
      }

      .form-group label {
        display: block;
        margin-bottom: 5px;
        font-size: 14px;
        font-weight: 500;
        color: #1a3c5e;
      }

      .form-group input,
      .form-group select {
        width: 100%;
        padding: 10px 12px 10px 40px; /* Adjusted for icons */
        border: 1px solid #000;
        border-radius: 20px;
        box-sizing: border-box;
        font-size: 14px;
        font-family: "Poppins", sans-serif;
      }

      .form-group input:focus,
      .form-group select:focus {
        border-color: #007bff;
        outline: none;
      }

      .form-group .icon {
        position: absolute;
        left: 10px;
        top: 65%;
        transform: translateY(-50%);
        color: #666;
        cursor: pointer;
        font-size: 16px;
      }

      input[type="submit"] {
        background-color: #f5c518;
        color: #1a3c5e;
        border: none;
        padding: 12px;
        border-radius: 20px;
        cursor: pointer;
        font-size: 16px;
        font-weight: 600;
        transition: background-color 0.3s ease;
        width: 100%;
        font-family: "Poppins", sans-serif;
      }

      input[type="submit"]:hover {
        background-color: #e0b015;
      }

      .form-options {
        display: flex;
        justify-content: center;
        align-items: center;
        margin-bottom: 10px;
        font-size: 14px;
      }

      .form-options a {
        color: #007bff;
        text-decoration: none;
        margin-left: 5px;
      }

      .form-options a:hover {
        text-decoration: underline;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }

      .error {
        color: red;
        text-align: center;
        margin-bottom: 10px;
      }

      input::placeholder {
        color: #999;
        font-style: italic;
      }
      .icon {
        margin-top: 4px;
        margin: 3px;
      }
      
      /* Responsive design for mobile */
      @media (max-width: 768px) {
        .form-container {
          flex-direction: column !important;
        }
        
        .form-container > div {
          flex: none !important;
          min-height: 200px;
        }
        
        .form-container > div:first-child {
          padding: 30px 20px !important;
        }
        
        .form-container > div:last-child {
          padding: 30px 20px !important;
        }
        
        .form-group-row {
          flex-direction: column;
          gap: 0;
        }
      }
    </style>
  </head>

  <body>
    <div class="form-container" style="display:flex; gap:0; max-width:980px; width:100%; padding:0; border-radius:14px; overflow:hidden; box-shadow:0 20px 40px rgba(0,0,0,0.25)">
      <!-- Brand panel -->
      <div style="flex:1; background: linear-gradient(135deg, rgba(255,107,107,0.95) 0%, rgba(78,205,196,0.90) 30%, rgba(69,183,209,0.85) 70%, rgba(150,206,180,0.90) 100%), radial-gradient(circle at 30% 20%, rgba(255,255,255,0.1) 0%, transparent 50%), radial-gradient(circle at 70% 80%, rgba(255,255,255,0.08) 0%, transparent 50%); color:#fff; display:flex; flex-direction:column; justify-content:center; align-items:center; padding:40px; position:relative; overflow:hidden;">
        <div style="font-size:46px; font-weight:700; letter-spacing:0.5px; display:flex; align-items:center; gap:12px;">
          <i class="fas fa-bus"></i>
          TransitNet
        </div>
        <div style="opacity:0.9; margin-top:10px; text-align:center; max-width:360px; line-height:1.5;">
          Create your account to manage routes, analyze mobility, and plan services.
        </div>
        <!-- Floating particles -->
        <div style="position:absolute; width:5px; height:5px; background:rgba(255,255,255,0.35); border-radius:50%; top:25%; left:20%; animation:float 7s ease-in-out infinite;"></div>
        <div style="position:absolute; width:7px; height:7px; background:rgba(255,255,255,0.25); border-radius:50%; top:60%; left:85%; animation:float 9s ease-in-out infinite reverse;"></div>
        <div style="position:absolute; width:4px; height:4px; background:rgba(255,255,255,0.4); border-radius:50%; top:35%; right:25%; animation:float 6s ease-in-out infinite;"></div>
      </div>
      <!-- Form panel -->
      <div style="flex:1; background:#fff; padding:42px 40px; display:flex; flex-direction:column; justify-content:center;">
        <h2 style="margin:0 0 6px 0">Create your account</h2>
        <p style="margin:0 0 20px 0; color:#6c757d">Get started with TransitNet Optimizer</p>
        <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger py-2" role="alert"><%= error %></div>
        <% } %>
        <form action="/signup" method="post">
          <div class="form-group-row">
            <div class="form-group" style="flex: 1">
              <label for="name">Name:</label>
              <input type="text" id="name" name="name" required autocomplete="off" placeholder="Enter your name" />
              <i class="fas fa-user icon"></i>
            </div>
            <div class="form-group" style="flex: 1">
              <label for="email">Email:</label>
              <input type="email" id="email" name="email" required autocomplete="off" placeholder="e.g. name@organization.gov" />
              <i class="fas fa-envelope icon"></i>
            </div>
          </div>
          <div class="form-group">
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" required autocomplete="off" placeholder="Enter your password" />
            <i class="fas fa-lock icon" id="togglePassword"></i>
          </div>
          <div class="form-group-row">
            <div class="form-group" style="flex: 1">
              <label for="userType">User Type:</label>
              <select id="userType" name="userType" required>
                <option value="">Select User Type</option>
                <option value="PolicyMaker">Policy Maker</option>
                <option value="Engineer">Engineer</option>
              </select>
              <i class="fas fa-caret-down icon"></i>
            </div>
            <div class="form-group" style="flex: 1">
              <label for="organization">Organization (optional):</label>
              <input type="text" id="organization" name="organization" autocomplete="off" placeholder="Enter organization" />
              <i class="fas fa-sitemap icon"></i>
            </div>
          </div>
          <div class="form-group" style="display:flex; justify-content:space-between; align-items:center; margin-top:10px;">
            <a href="/login" style="font-size:13px; color:#0d6efd; text-decoration:none;">Already have an account?</a>
            <input type="submit" value="Sign Up" />
          </div>
        </form>
      </div>
    </div>
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        const form = document.querySelector("form");
        const passwordInput = document.getElementById("password");

        document.getElementById("togglePassword").addEventListener("click", function () {
          if (passwordInput.type === "password") {
            passwordInput.type = "text";
            this.classList.remove("fa-lock");
            this.classList.add("fa-lock-open");
          } else {
            passwordInput.type = "password";
            this.classList.remove("fa-lock-open");
            this.classList.add("fa-lock");
          }
        });

        form.addEventListener("submit", function (event) {
          const password = passwordInput.value;
          if (password.length < 7) {
            event.preventDefault();
            alert("Password must be at least 7 characters long.");
          }
        });
      });
    </script>
  </body>
</html>
